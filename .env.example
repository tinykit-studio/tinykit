# ===========================================
# tinykit Configuration (Local Development)
# ===========================================
# For Railway deployment, see .env.railway

# AI Agent Configuration
# You can configure LLM settings here OR in the tinykit UI (Settings page)
# Env vars take precedence over UI settings if both are set
# Choose your provider: openai, anthropic, gemini, zai, or ollama
LLM_PROVIDER=anthropic
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-20250514

# Optional: Custom base URL for OpenAI-compatible providers
# LLM_BASE_URL=https://api.z.ai/api/coding/paas/v4

# OpenAI: Get key from https://platform.openai.com/api-keys
# Anthropic: Get key from https://console.anthropic.com/
# Gemini: Get key from https://aistudio.google.com/apikey
# z.ai: Get key from https://z.ai

# Example configurations:
# For OpenAI GPT-4:
#   LLM_PROVIDER=openai
#   LLM_API_KEY=sk-...
#   LLM_MODEL=gpt-4

# For Anthropic Claude:
#   LLM_PROVIDER=anthropic
#   LLM_API_KEY=sk-ant-...
#   LLM_MODEL=claude-sonnet-4.5

# For Google Gemini:
#   LLM_PROVIDER=gemini
#   LLM_API_KEY=your-gemini-api-key
#   LLM_MODEL=gemini-3-pro-preview

# For z.ai:
#   LLM_PROVIDER=zai
#   LLM_API_KEY=your-zai-key
#   LLM_MODEL=glm-4.6
#   LLM_BASE_URL=https://api.z.ai/api/paas/v4

# For Ollama (local):
#   LLM_PROVIDER=ollama
#   LLM_MODEL=llama2

# ===========================================
# Server Configuration (OPTIONAL)
# ===========================================
PORT=5173
HOST=0.0.0.0

# External PocketBase URL (only if not using built-in)
# POCKETBASE_URL=http://127.0.0.1:8091

# ===========================================
# Security (OPTIONAL but RECOMMENDED for production)
# ===========================================
# Set a password to protect the /tinykit builder interface
# This also creates the PocketBase superuser (admin@localhost)
# If not set, builder and PocketBase admin are open (for local dev)
# ADMIN_PASSWORD=your-secure-password-here

# Simple password protection for your generated app (Alpha)
# Used to protect /api/* routes and prevent unauthorized access
# User will be prompted for password on first visit (window.prompt)
# Good for hosting personal tools on public domains
# APP_PASSWORD=your-app-password-here
